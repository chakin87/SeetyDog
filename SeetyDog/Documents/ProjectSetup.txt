#pragma once

Libraries----------------------------------------------------------------


glm - Math Library. 
	Used Git - no premake needed because it's a header only library
		git submodule add https://github.com/g-truc/glm SeetyDog/externlibs/glm
